# Mercury playground

Test project for mucking around with Mercury

# Running

Required: NodeJS, Browserify

* ./setup.sh to install the libraries used
* ./build.sh to create the Javascript bundle
* ./run.sh to start a Python HTTP server on http://localhost:8087